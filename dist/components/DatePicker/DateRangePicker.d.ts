import { ChangeDetectorRef } from "angular2/core";
import { EventEmitter } from "angular2/core";
import { DateRange } from "../../utilities/DateUtils";
import { DatePicker } from "./DatePicker";
import { InfiniteScroller } from "../InfiniteScroller/InfiniteScroller";
export declare class DateRangePicker extends DatePicker {
    valueChange: EventEmitter<{}>;
    value: any;
    minDate: Date | string;
    maxDate: Date | string;
    dateFilter: (d: Date) => boolean;
    calendarScroller: InfiniteScroller;
    private _dateTarget;
    calendarHeight: string;
    selectedDate: Date;
    startLabel: string;
    endLabel: string;
    private _startDate;
    private _endDate;
    startDateChange: EventEmitter<{}>;
    startDate: any;
    endDateChange: EventEmitter<{}>;
    endDate: any;
    private _inputStartDate;
    inputStartDate: string;
    private _inputEndDate;
    inputEndDate: string;
    constructor(changeDetector: ChangeDetectorRef);
    handleRangeInput(value: any): DateRange;
    focusStartDate(): void;
    focusEndDate(): void;
    checkStartDateTarget(): boolean;
    checkEndDateTarget(): boolean;
}
